# Notes

## 未確定事項

- `05_curation` の人手編集をどう定義するか
  - `category_master_draft.csv` を人が編集して `category_master.csv` を作る方針までは固まっている
  - ただし、クラスタの統合・分割・改名をどの成果物で表現するかは未確定
- カテゴリ候補の LLM 草案を標準フローに含めるか
  - 現状の `curation.py` は草案生成を持っている
  - 一方で、BERTopic が見つけた山に人が名前を付けるだけで十分ではないか、という論点が残っている
- `06_classification` の最終分類方式
  - 現状実装は「回答 embedding とカテゴリ embedding の近さ」で再分類している
  - ただし、期待している流れは「BERTopic の山を人が整理し、その結果を全件へ再適用する」方向に近い可能性がある
  - 後段で別方式を再発明していないかは引き続き見直し対象
